package flaxbeard.immersivepetroleum.common.multiblocks;

// TODO this is a hack, ideally we'd remove the non-API dependency here
@Deprecated(forRemoval = true)
public abstract class IPTemplateMultiblock //extends IETemplateMultiblock
{
/*	private final MultiblockRegistration<? extends IMultiblockState> baseState;
	
	public IPTemplateMultiblock(ResourceLocation loc, BlockPos masterFromOrigin, BlockPos triggerFromOrigin, BlockPos size, MultiblockRegistration<? extends IMultiblockState> baseState){
		// TODO check that this doesn't run too early to query the RegObject
		super(loc, masterFromOrigin, triggerFromOrigin, size, baseState);
		this.baseState = baseState;
	}
	
	public Block getBaseBlock(){
		return baseState.block().get();
	}
*/}
