package flaxbeard.immersivepetroleum.common.blocks.metal;

@Deprecated(forRemoval = true)
public class OilTankBlock //extends IPMetalMultiblock<OilTankTileEntity>
	{
	/*public OilTankBlock(){
		super(IPTileTypes.OILTANK);
	}
	
	@Override
	public boolean isLadder(BlockState state, LevelReader world, BlockPos pos, LivingEntity entity){
		BlockEntity te = world.getBlockEntity(pos);
		if(te instanceof IMultiblockBE<?> multiblockBE && multiblockBE.getHelper().getContext().getState() instanceof OilTankLogic.State mbState && mbState.isLadder()){
			return true;
		}
		return false;
	}*/
}
